<div class="content grid">
  <section class="span-8">

    <h2>Confirm account</h2>
    <p>Enter your email address to resend account confirmation instructions.</p>

    <%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post, class: 'form' }) do |form| %>

      <%= render 'form_field', locals: {
        resource: resource,
        form: form,
        field_name: :email,
        field_type: :email_field,
        label: 'Email address',
        options: { autofocus: true, autocomplete: 'email', class: 'text-input large' }
      } %>

      <div class="actions">
        <%= form.submit 'Submit', class: 'button' %>
        <%= link_to 'Sign in', new_user_session_path %>
      </div>

    <% end %>

  </section>
</div>
