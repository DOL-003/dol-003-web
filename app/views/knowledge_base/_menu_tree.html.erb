<ul>
  <% locals[:tree].each do |item| %>
    <li>
      <%= link_to item[:title], item[:path] %>
      <%= menu_tree(item[:children], item[:path]) if item[:children].present? %>
    </li>
  <% end %>
</ul>
